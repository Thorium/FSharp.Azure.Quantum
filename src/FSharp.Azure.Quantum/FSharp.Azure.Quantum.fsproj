<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GenerateProgramFile>false</GenerateProgramFile>

    <!-- Package Metadata -->
    <Version>1.4.7</Version>
    <PackageId>FSharp.Azure.Quantum</PackageId>
    <Title>FSharp.Azure.Quantum</Title>
    <Description>F# library for Azure Quantum with quantum-first optimization (Graph Coloring, TSP, MaxCut, Knapsack, Network Flow, Portfolio), Azure Quantum backend integration (IonQ, Rigetti, Quantinuum, Atom Computing), extensible circuit validation, local quantum simulation, QAOA with automatic parameter optimization (Nelder-Mead), OpenQASM 2.0 support, error mitigation techniques (ZNE, PEC, REM), Quantum Random Number Generation (QRNG), and educational quantum algorithms (Grover, QFT, Amplitude Amplification) with cloud backend execution. Native IQuantumBackend integration for IonQ, Rigetti, Quantinuum, Atom Computing, IQM and D-Wave, plus Pasqal neutral-atom analog hardware (via Pulser) and a neutral-atom analog programming model; additional platforms (IBM, Amazon Braket, Google Cirq) are targetable via OpenQASM 2.0 export/import.</Description>
    <Summary>F# library for Azure Quantum with quantum-first optimization, quantum backends (IonQ, Rigetti, Quantinuum, Atom Computing), QAOA with automatic parameter tuning, QRNG, and educational algorithms with cloud execution</Summary>
    <PackageReleaseNotes>v1.4.7: Correctness release — second-pass audit (silently-wrong-results fixes + dependency updates)

BREAKING CHANGES (vs 1.4.6):
- QuantumClientConfig gained a required Location field (Azure region, e.g. "eastus") and Client.createConfig takes it as a new parameter. QuantumClient now targets the {location}.quantum.azure.com DATA-PLANE endpoint — the previous management.azure.com (ARM) base URL made every live job call fail 401/404 against the quantum-scoped bearer token
- Amplitude encoding (FeatureMap.amplitudeEncoding) prepares the state with amplitude[i] = normalizedFeature[i] (state index = feature index). The previous construction was wrong for most inputs (skipped Gray-code CNOTs, wrong closing control, missing Gray reindexing of the multiplexed-RY angles) — kernels/VQC models trained on amplitude-encoded data should be retrained

FIXED (silently wrong results):
- CRX transpilation was the identity (X commutes with RX, so the RX-CNOT ladder cancelled) — now decomposed as H·CRZ(θ)·H; any transpiled circuit containing CRX was silently missing the gate
- Amplitude amplification's reflection about the prepared state executed A;S0;A† (operator A†·S0·A) instead of A†;S0;A — correct only for self-inverse preparations such as plain Grover's H⊗n
- HHL general (non-diagonal) path maps upper-half QPE register values to NEGATIVE eigenvalues (phase unwrap); a λ &lt; 0 component was previously inverted as phaseScale − |λ|. Phase scale is now 4·maxEig (positive and negative eigenvalues in disjoint phase halves; costs one bit of QPE precision)
- OpenQASM angle formatting in CircuitBuilder.toOpenQASM and QaoaCircuit.toOpenQasm uses InvariantCulture F10 (comma-decimal locales emitted "rx(1,57)" — invalid QASM submitted to paid hardware)
- QuantumClient.GetResultsAsync downloads results from outputDataUri (blob SAS URL) — the job model never inlines outputData, so the previous code could not return results
- SAS blob downloads (job results, Client + JobLifecycle) no longer carry the workspace bearer token: Azure Storage rejected it (401) and the token leaked to the storage host (AuthenticationHandler honors a per-request no-auth marker)
- VQC regression models persist ValueRange (ValueRangeMin/Max in SerializableVQCModel) — reloaded models previously produced affinely-rescaled predictions

FIXED (crashes / robustness):
- Rate-limit exponential backoff overflowed Int32 after ~23 consecutive 429s (Random.Next(min &gt; max) crashed the HTTP pipeline); backoff and job-poll delays now honor cancellation tokens
- Sync ExecuteToState wrappers on the five cloud backends run via Task.Run — no more permanent deadlock when called from a UI thread (WPF/WinForms)
- QAOA multi-start optimization no longer runs two extra objective evaluations (paid quantum jobs) per start for a log line, and only evaluates when a logger is attached
- HttpResponseMessage/JsonDocument disposal in QuantumClient and the D-Wave SAPI backend (socket exhaustion under load)
- Guards: correlation/covariance matrices over return series with no common dates; SMO training with a single sample; confusion matrix with test labels unseen at training; measurement standard deviation NaN from one-ulp negative variance; risk engine rejects SimulationPaths &lt; 2 with a validation error

UPDATED: FSharp.Core 10.1.302, Microsoft.Extensions.Logging 10.0.10, OpenTelemetry 1.17.0, Serilog 4.4.0

v1.4.6: Correctness release — full-library audit (25 critical + ~35 high-severity fixes)

BREAKING CHANGES (vs 1.4.5):
- submitAndWaitForResultsAsync takes a trailing CancellationToken on IonQ/Rigetti/Quantinuum/AtomComputing/Pasqal backends (polling is now cancellable)
- QaoaOptimizer.Optimizer.minimizeWithBounds takes tolerance and maxIterations parameters (config values are now honored; non-convergence returns best-so-far instead of throwing)
- QuantumState gained a MeasurementHistogram case (and QuantumStateType.Sampled) — exhaustive matches must handle or wildcard it
- MitigationTechnique.ReadoutErrorMitigation payload is now CalibrationMatrix option (no fabricated default calibration; missing calibration = pass-through)
- QuantumGraphColoringSolver.solveClassical returns Result instead of throwing when colors run out
- constraintSolver's searchSpace now means NUMBER OF VARIABLES (matching forSudokuStyle/forNQueens); numVariables × log2(domainSize) ≤ 16 qubits is enforced with a clear error
- Knapsack/set-cover/drug-discovery QUBOs use slack variables for inequality constraints (NumVariables grew; decoders unchanged for item bits)
- findCheapestBackend/recommendCostOptimization exclude subscription-priced backends (Quantinuum has no per-job USD price); an all-subscription backend list returns an Error / no recommendation
- Business classification/regression builders REJECT feature counts above the qubit cap (8 binary / 5 predictive) with a clear error instead of failing every epoch (1.4.5) or silently truncating; reduce dimensionality first
- MitigatedResult gained CorrectionApplied: false means the histogram passed through uncorrected (readout mitigation selected without calibration data)

NEW: Wide cloud hardware support
- QuantumState.MeasurementHistogram: sampled results with NO qubit-count limit (at most `shots` entries regardless of width)
- Cloud result tiering everywhere (Braket + Azure backends): dense state ≤ 20 qubits, sparse state 21-31, measurement histogram beyond — Quantinuum H2 (56q), Rigetti Ankaa (~84q), IonQ Forte (36q) results are now usable (previously crashed above 20)

HIGH-severity fixes (selection):
- IonQ result parser accepts the documented Azure probability-histogram format (decimal state keys); live jobs no longer error
- OpenQASM export defines rxx/ryy/rzz inline where qelib1/stdgates lack them; importer accepts the definitions
- VQC gradients apply the chain rule (parameter-shift on circuit expectations × analytic loss derivative) — was a biased finite difference of the loss
- VQC model serialization stores PauliFeatureMap/TwoLocal configuration losslessly (saveVQCModelWithConfigAsync); AutoML train/validation split is seeded-shuffled
- Parametric VaR honors the requested confidence for Student-t/log-normal; Historical redirects to the empirical function; Asian options priced via the exact geometric-average distribution (was silently European)
- Network-flow conservation penalty de-doubled and solutions validated (conservation/supply/demand) before selection; graph-coloring fixed colors enforced; scheduling objective (incl. MinimizeLateness) actually honored
- Portfolio: negative-return zero-risk assets no longer rank first; quantum decode respects Min/MaxHolding; KnapsackBuilder solveClassicalDP is genuine dynamic programming
- Shor draws a fresh random base per retry (N=33-style permanent failures fixed; gcd hits return the factor immediately)
- QRNG local path uses a cryptographic RNG and documentation states it is CSPRNG, not quantum, randomness
- Molecular CSV labels stay aligned; unlabeled drug screening errors instead of reporting every candidate as a hit; malformed SMILES fail at parse time
- Cost recommendations exclude subscription-priced backends (Quantinuum HQC no longer 'cheapest' at $0); QAOA optimizer non-convergence returns best-so-far
- Density-matrix simulator handles Reset/Barrier/Conditional with exact channels; strategy selection no longer allocates a 2^n×2^n placeholder (34 GB at 16 qubits)

v1.4.6 critical fixes (earlier batch):
- FIXED: Circuit adjoint (CircuitBuilder.reverse) now reverses gate order — corrects amplitude amplification reflections and quantum Monte Carlo Grover operators
- FIXED: Multi-controlled X/Z decomposition for 3+ controls (correct ancilla-free recursion; the old "borrowed ancilla" ladder computed the wrong predicate) — affects Grover 4+ qubits and HHL eigenvalue inversion
- FIXED: QUBO -&gt; Ising conversion (fromQubo/fromQuboSparse) now includes cross-term linear contributions, and QuboExtraction is its exact inverse (D-Wave path optimized a corrupted objective before)
- FIXED: Trotter-Suzuki Pauli decomposition qubit ordering (was bit-reversed vs circuit synthesis) and diagonal decomposition (correct I/Z expansion via Walsh-Hadamard transform); HHL controlled-evolution gate order; controlled evolution of identity terms emits the control phase
- FIXED: Zero-noise extrapolation scaled circuits executed in reverse gate order
- FIXED: Budget checks no longer skip the monthly limit when a daily warning fires
- FIXED: Task scheduling — resource capacity encoded with non-negative overload penalties (equality penalty rewarded infeasible schedules); decoded schedules re-validated classically (one-hot, dependencies, resource capacity) with earliest-slot one-hot repair
- FIXED: Grover constraint solver — searchSpace is the number of variables (matching forSudokuStyle/forNQueens), with a 16-qubit feasibility guard; mixed-radix decode no longer overflows
- FIXED: Graph coloring greedy solver returns Result instead of throwing when colors run out (HybridSolver propagates)
- FIXED: ML — amplitude-encoding Walsh-Hadamard butterfly (wrong state for 3+ qubits), 2-class VQC prediction (always returned first class), anomaly detection rebuilt on kernel centroid distance (one-class SVM was degenerate and could never learn); model loaders validate model kind (wrong-kind files no longer load as VQC-regression with null parameters)
- FIXED: Invariant-culture number parsing in chemistry and market data (comma-decimal locales silently lost elements/atoms/returns)
- FIXED: Parametric Expected Shortfall no longer overstated by factor z (~2.33x at 99%); risk engine errors on missing/malformed data instead of fabricating zeros or mock returns
- FIXED: D-Wave direct backend speaks the documented SAPI v2 qp wire format (base64 lin/quad against solver topology; correct submit/poll/answer decoding); problems off the hardware graph error explicitly (minor-embedding not implemented)
- NOTE: P(theta) transpiles to RZ(theta) on backends without a native phase gate (equal up to global phase)

v1.4.3: Neutral-atom analog mode, CUDA-Q-style primitives, and new backends
- NEW: Neutral-atom (Rydberg) analog programming - NeutralAtom module Trotterizes an analog program to gates; PasqalBackend (RydbergProgram -&gt; Pulser JSON -&gt; Azure Quantum) and QueraBackend (RydbergProgram -&gt; Braket AHS JSON)
- NEW: CUDA-Q-style execution primitives (Primitives: sample/observe/run/getState) over IQuantumBackend, plus CudaQBridge
- NEW: ADAPT-VQE and ADAPT-QAOA adaptive-ansatz algorithms (AdaptVqe, AdaptQaoa)
- NEW: IQM gate backend (IqmBackend)
- NEW: Density-matrix noisy simulator backend (NoisyBackend)
- NEW: Emulate-target mode (Emulation) - transpile, validate, then run locally for a cloud target
- NEW: Companion FSharp.Azure.Quantum.Braket package isolates the AWS SDK dependency (QuEra neutral-atom analog and Braket gate QPUs)
- IMPROVED: Business builders and ML model serialization refactors

v1.4.2: Compilation/hardware tooling and weighted MAX-SAT
- NEW: QubitRouting - inserts SWAPs so two-qubit gates respect device connectivity (CouplingMap: grid/linear/fromPairs); tracks logical-to-physical qubit permutation
- NEW: CloudBackends.Factory.createRigettiRouted - Rigetti backend that auto-routes circuits for a device coupling map
- NEW: QirEmitter - emit circuits as QIR base-profile textual LLVM IR for Azure Quantum submission
- NEW: ResourceEstimation - logical (qubits/gates/T-count/depth) and physical (surface-code) resource estimates
- NEW: NoiseModel - DeviceNoiseProfile with noise-aware routing (routeNoiseAware) and success-probability estimation
- NEW: Weighted MAX-SAT - per-clause weights in QuantumSatSolver (clause / weightedClause); solution reports SatisfiedWeight/TotalWeight
- IMPROVED: AutoML returns a typed TrainedModel discriminated union instead of obj (no unboxing in predict)
- IMPROVED: Connected-component decomposition now unions sub-solutions for Vertex Cover and Maximum Matching
- IMPROVED: Knapsack builder shares the solver item type (no duplicate record or field mapping)

v1.4.1: Dependency updates
- UPDATED: Azure.Identity 1.13.1 to 1.21.0
- UPDATED: FSharp.Core to 10.1.301
- UPDATED: Microsoft.Extensions.Logging to 10.0.9, Serilog to 4.3.1
- UPDATED: OpenTelemetry to 1.16.0 (Exporter.Console 1.16.0, Instrumentation.Http 1.15.1)

v1.3.10: Async backend refactoring
- NEW: Task-based async APIs on IQuantumBackend (ExecuteToStateAsync, ApplyOperationAsync) with CancellationToken
- NEW: Async QAOA execution helpers (executeQaoaCircuitAsync, executeQaoaWithGridSearchAsync, executeWithBudgetAsync)
- NEW: Async ML pipeline (computeKernelAsync, computeKernelMatrixAsync, predictAsync, evaluateAsync)
- NEW: Cloud backend factory (CloudBackendFactory) for Rigetti, IonQ, Quantinuum, AtomComputing
- NEW: UnifiedBackend async helpers (applySequenceAsync, applyWithConversionAsync)
- NEW: OpenQASM export async (exportToFileAsync, exportToFileWithConfigAsync)
- IMPROVED: All async methods use task {} CE (not async {}), returning Task&lt;T&gt; for .NET interop
- IMPROVED: Sync methods marked [Obsolete] where async alternatives exist
- FIXED: 6 pre-existing .fsx script failures (MathNet dependency, syntax errors)
- Status: Production-ready</PackageReleaseNotes>

    <!-- Repository Info -->
    <RepositoryUrl>https://github.com/thorium/FSharp.Azure.Quantum</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/thorium/FSharp.Azure.Quantum</PackageProjectUrl>

    <!-- Author & License -->
    <Authors>Tuomas Hietanen</Authors>
    <PackageTags>F#;FSharp;Quantum;Azure;Computing;Optimization;TSP;Portfolio;QAOA;Circuit</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageLicenseExpression>Unlicense</PackageLicenseExpression>
    <Copyright>Unlicense - Public Domain</Copyright>

    <!-- Symbols -->
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <IncludeSymbols>true</IncludeSymbols>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
	<PackageIcon>logo.png</PackageIcon>

  </PropertyGroup>

  <ItemGroup>
    <!-- Test Visibility: Allow tests to call internal functions -->
    <InternalsVisibleTo Include="FSharp.Azure.Quantum.Tests" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="/" />
		<None Include="..\..\docs\images\logo.png" Pack="true" PackagePath="/" />
  </ItemGroup>

  <ItemGroup>
    <!-- Core Layer - Foundation Types -->
    <Compile Include="Core\QuantumError.fs" />
    <Compile Include="Core\Validation.fs" />
    <Compile Include="Core\Types.fs" />
    <Compile Include="Core\Progress.fs" />
    <Compile Include="Core\ResultBuilder.fs" />

    <!-- Data Infrastructure Layer (depends on Core) -->
    <Compile Include="Data\PeriodicTable.fs" />
    <Compile Include="Data\MoleculeLibrary.fs" />
    <Compile Include="Data\MoleculeFormats.fs" />
    <Compile Include="Data\ChemistryDataProviders.fs" />
    <Compile Include="Data\MolecularData.fs" />
    <Compile Include="Data\SmilesDataProviders.fs" />
    <Compile Include="Data\FinancialData.fs" />

    <!-- QUBO Helpers - Shared patterns for optimization solvers -->
    <Compile Include="Qubo.fs" />

    <!-- Local Simulator Layer (depends on Types) -->
    <Compile Include="LocalSimulator\StateVector.fs" />
    <Compile Include="LocalSimulator\Gates.fs" />
    <Compile Include="LocalSimulator\Measurement.fs" />
    <Compile Include="LocalSimulator\QaoaSimulator.fs" />

    <!-- Unified Backend Architecture - Core State Types (depends on LocalSimulator) -->
    <Compile Include="Core\QuantumState.fs" />
    <Compile Include="Core\QuantumStateConversion.fs" />

    <!-- Quantum Algorithms (depends on LocalSimulator) -->
    <Compile Include="Algorithms\GroverOracle.fs" />
    <Compile Include="Algorithms\ShorsTypes.fs" />


    <!-- Core Infrastructure (depends on Types, LocalSimulator) -->
    <Compile Include="Core\CostEstimation.fs" />
    <Compile Include="Core\CircuitValidator.fs" />
    <!-- RateLimiting precedes Authentication so createAuthenticatedClient can compose ThrottlingHandler into the HTTP handler chain -->
    <Compile Include="Core\RateLimiting.fs" />
    <Compile Include="Core\Authentication.fs" />
    <Compile Include="Core\Retry.fs" />
    <Compile Include="Core\Observability.fs" />
    <Compile Include="Core\Batching.fs" />
    <Compile Include="Core\JobLifecycle.fs" />

    <!-- Backends Layer (depends on Core Infrastructure) -->
    <Compile Include="Backends\DWaveTypes.fs" />
    <Compile Include="Backends\IonQBackend.fs" />
    <Compile Include="Backends\RigettiBackend.fs" />
    <Compile Include="Backends\QuantinuumBackend.fs" />
    <Compile Include="Backends\AtomComputingBackend.fs" />
    <Compile Include="Backends\IqmBackend.fs" />
    <Compile Include="Backends\AzureQuantumWorkspace.fs" />
    <Compile Include="Core\Client.fs" />

    <!-- D-Wave Integration (depends on DWaveTypes) -->
    <Compile Include="Algorithms\QuboToIsing.fs" />

    <!-- Classical Solvers - Pure CPU algorithms, no quantum backend (depends on Types) -->
    <Compile Include="Solvers\Classical\TspTypes.fs" />
    <Compile Include="Solvers\Classical\TspSolver.fs" />
    <Compile Include="Solvers\Classical\PortfolioTypes.fs" />
    <Compile Include="Solvers\Classical\PortfolioSolver.fs" />

    <!-- Builders Layer (depends on Types, LocalSimulator) -->
    <Compile Include="Builders\CircuitBuilder.fs" />
    <Compile Include="Builders\GateTranspiler.fs" />
    <Compile Include="Builders\QubitRouting.fs" />
    <Compile Include="Builders\ResourceEstimation.fs" />
    <Compile Include="Builders\NoiseModel.fs" />
    <Compile Include="Builders\QirEmitter.fs" />
    <Compile Include="Builders\CudaQBridge.fs" />
    <Compile Include="Builders\OpenQasmVersion.fs" />
    <Compile Include="Builders\OpenQasmExport.fs" />
    <Compile Include="Builders\OpenQasmImport.fs" />
    <Compile Include="Builders\QuboEncoding.fs" />
    <Compile Include="Builders\GraphOptimization.fs" />

    <!-- Error Mitigation Layer (depends on Builders) -->
    <Compile Include="ErrorMitigation\ZeroNoiseExtrapolation.fs" />
    <Compile Include="ErrorMitigation\ProbabilisticErrorCancellation.fs" />
    <Compile Include="ErrorMitigation\ReadoutErrorMitigation.fs" />
    <Compile Include="ErrorMitigation\ErrorMitigationStrategy.fs" />

    <!-- QAOA Infrastructure (depends on CircuitBuilder, LocalSimulator) -->
    <Compile Include="Core\QaoaCircuit.fs" />
    <Compile Include="Core\CircuitAbstraction.fs" />
    <Compile Include="Core\BackendAbstraction.fs" />
    <Compile Include="Core\QaoaOptimizer.fs" />
    <Compile Include="Core\QaoaExecutionHelpers.fs" />
    <Compile Include="Core\ProblemDecomposition.fs" />
    <Compile Include="Core\QaoaParameterOptimizer.fs" />

    <!-- Unified backend-aware educational amplitude amplification (depends on BackendAbstraction + Builders) -->
    <Compile Include="Algorithms\AmplitudeAmplification.fs" />

    <!-- Local Backend (depends on CircuitBuilder, CircuitAbstraction, BackendAbstraction, UnifiedBackendAbstraction) -->
    <Compile Include="Backends\LocalBackend.fs" />

    <!-- Density-matrix noisy simulator backend (depends on LocalBackend, CircuitAbstraction) -->
    <Compile Include="Backends\NoisyBackend.fs" />

    <!-- Cloud Backend IQuantumBackend Wrappers (depends on BackendAbstraction, CircuitAbstraction, OpenQasmExport, utility backend modules) -->
    <Compile Include="Backends\CloudBackendHelpers.fs" />
    <Compile Include="Backends\CloudBackends.fs" />

    <!-- Unified Backend Algorithms (depends on CircuitBuilder, UnifiedBackendAbstraction, LocalBackend) -->
    <Compile Include="Algorithms\BellStates.fs" />
    <Compile Include="Algorithms\SuperdenseCoding.fs" />
    <Compile Include="Algorithms\DeutschJozsa.fs" />
    <Compile Include="Algorithms\BernsteinVazirani.fs" />
    <Compile Include="Algorithms\Simon.fs" />
    <Compile Include="Algorithms\QuantumKeyDistribution.fs" />
    <Compile Include="Algorithms\EkertQKD.fs" />
    <Compile Include="Algorithms\QuantumTeleportation.fs" />
    <Compile Include="Algorithms\Grover.fs" />
    <Compile Include="Algorithms\TreeSearch.fs" />
    <Compile Include="Algorithms\QFT.fs" />
    <!-- State preparation must precede QPE: custom eigenvector prep uses Mottonen -->
    <Compile Include="Algorithms\MottonenStatePreparation.fs" />
    <Compile Include="Algorithms\QPE.fs" />

    <!-- Unified Quantum Arithmetic (state-based, works with IQuantumBackend) -->
    <!-- ✅ Migration Complete: All modular arithmetic functions implemented (Dec 11, 2024) -->
    <!-- Must come before Shor.fs: Shor's controlledModularMultiplication calls Arithmetic module -->
    <Compile Include="Algorithms\QuantumArithmetic.fs" />

    <Compile Include="Algorithms\Shor.fs" />
    <Compile Include="Algorithms\TrotterSuzuki.fs" />

    <!-- CUDA-Q-style execution primitives (sample/observe/run/getState) over IQuantumBackend.
         Depends on TrotterSuzuki (PauliHamiltonian) for observe. -->
    <Compile Include="Core\Primitives.fs" />

    <!-- ADAPT-VQE / ADAPT-QAOA adaptive ansatz (depends on Primitives, TrotterSuzuki, QaoaOptimizer) -->
    <Compile Include="Algorithms\AdaptVqe.fs" />
    <Compile Include="Algorithms\AdaptQaoa.fs" />

    <!-- Emulate-target mode (transpile→validate→local-run); depends on Primitives, GateTranspiler, CircuitValidator, LocalBackend -->
    <Compile Include="Core\Emulation.fs" />

    <!-- Neutral-atom (Rydberg) analog mode: Trotterizes an analog program to gates (depends on Primitives) -->
    <Compile Include="Algorithms\NeutralAtom.fs" />

    <!-- Pasqal neutral-atom cloud backend: RydbergProgram -> Pulser JSON -> Azure Quantum -->
    <Compile Include="Algorithms\PasqalBackend.fs" />

    <!-- QuEra neutral-atom via AWS Braket: RydbergProgram -> Braket AHS program JSON -->
    <Compile Include="Algorithms\QueraBackend.fs" />
    <Compile Include="Algorithms\QuantumErrorCorrection.fs" />
    <Compile Include="Algorithms\HHLTypes.fs" />
    <Compile Include="Algorithms\HHL.fs" />

    <!-- Statistical distributions for quantum algorithms (option pricing, ML, etc.) -->
    <Compile Include="Algorithms\StatisticalDistributions.fs" />
    <!-- QAOA Algorithm (depends on QaoaCircuit, CircuitAbstraction, QaoaOptimizer) -->
    <Compile Include="Algorithms\QRNG.fs" />
    <!-- Quantum-enhanced statistical distributions (depends on QRNG, StatisticalDistributions) -->
    <Compile Include="Algorithms\QuantumDistributions.fs" />
    <Compile Include="Algorithms\QuantumMonteCarlo.fs" />
    <!-- D-Wave QUBO Extraction (depends on QaoaCircuit, CircuitAbstraction) -->
    <Compile Include="Algorithms\QuboExtraction.fs" />

    <!-- D-Wave Backend (depends on QuboExtraction, QuboToIsing, DWaveTypes, BackendAbstraction) -->
    <Compile Include="Backends\DWaveBackend.fs" />
    <Compile Include="Backends\RealDWaveBackend.fs" />

        <!-- Quantum Solvers - Need conversion to ExecuteToState API -->
    <Compile Include="Solvers\Quantum\QuantumTspSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumNetworkFlowSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumPortfolioSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumMaxCutSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumKnapsackSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumGraphColoringSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumArithmeticBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumConstraintSolverBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumPatternMatcherBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumTreeSearchBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumPeriodFinderBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumPhaseEstimatorBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumLinearSystemSolverBuilder.fs" />
    <Compile Include="Solvers\Quantum\QuantumChemistry.fs" />
    <Compile Include="Solvers\Quantum\DrugDiscoverySolvers.fs" />
    <!-- Expansion solvers (Phase 1: Graph optimization) -->
    <Compile Include="Solvers\Quantum\QuantumVertexCoverSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumCliqueSolver.fs" />
    <!-- Expansion solvers (Phase 2: Non-graph optimization) -->
    <Compile Include="Solvers\Quantum\QuantumSetCoverSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumSatSolver.fs" />
    <!-- Expansion solvers (Phase 3: Medium priority) -->
    <Compile Include="Solvers\Quantum\QuantumMatchingSolver.fs" />
    <Compile Include="Solvers\Quantum\QuantumBinPackingSolver.fs" />
    <!-- Expansion solvers (Phase 4: Stretch goal) -->
    <Compile Include="Solvers\Quantum\QuantumBinaryILPSolver.fs" />

    <!-- Hybrid Solvers - Automatic routing between classical and quantum -->
    <Compile Include="Solvers\Hybrid\ProblemAnalysis.fs" />
    <Compile Include="Solvers\Hybrid\QuantumAdvisor.fs" />
    <Compile Include="Solvers\Hybrid\HybridSolver.fs" />

    <!-- Task Scheduling - Domain-specific API for scheduling problems -->
    <Compile Include="TaskScheduling\Types.fs" />
    <Compile Include="TaskScheduling\Validation.fs" />
    <Compile Include="TaskScheduling\Builders.fs" />
    <Compile Include="TaskScheduling\ScheduleMetrics.fs" />
    <Compile Include="TaskScheduling\ClassicalSolver.fs" />
    <Compile Include="TaskScheduling\QuboEncoding.fs" />
    <Compile Include="TaskScheduling\QuantumSolver.fs" />
    <Compile Include="TaskScheduling\Export.fs" />
    <Compile Include="TaskScheduling\TaskScheduling.fs" />

    <!-- Classical Builders - Domain-specific builder APIs -->
    <Compile Include="Solvers\Classical\TspBuilder.fs" />
    <Compile Include="Solvers\Classical\PortfolioBuilder.fs" />
    <Compile Include="Solvers\Classical\NetworkFlowBuilder.fs" />
    <Compile Include="Solvers\Classical\MaxCutBuilder.fs" />
    <Compile Include="Solvers\Classical\KnapsackBuilder.fs" />
    <Compile Include="Solvers\Classical\GraphColoringBuilder.fs" />

    <!-- Financial Derivatives - Quantum Monte Carlo for Option Pricing -->
    <Compile Include="Business\OptionPricingBuilder.fs" />

    <!-- Utilities Layer (depends on Classical Builders) -->
    <Compile Include="Utilities\PerformanceBenchmarking.fs" />

    <!-- Visualization Layer (zero dependencies, pure F#) -->
    <Compile Include="Visualization\Types.fs" />
    <Compile Include="Visualization\ASCIIRenderer.fs" />
    <Compile Include="Visualization\MermaidRenderer.fs" />
    <Compile Include="Visualization\CircuitExtensions.fs" />
    <Compile Include="Visualization\SolutionExtensions.fs" />

    <!-- Quantum Machine Learning (depends on CircuitBuilder, BackendAbstraction) -->
    <Compile Include="MachineLearning\QMLTypes.fs" />
    <Compile Include="MachineLearning\FeatureMap.fs" />
    <Compile Include="MachineLearning\VariationalForm.fs" />
    <Compile Include="MachineLearning\AdamOptimizer.fs" />
    <Compile Include="MachineLearning\QuantumKernel.fs" />
    <Compile Include="MachineLearning\QuantumKernelSVM.fs" />
    <Compile Include="MachineLearning\VQC.fs" />
    <Compile Include="MachineLearning\MultiClassSVM.fs" />
    <Compile Include="MachineLearning\SVMModelSerialization.fs" />
    <Compile Include="MachineLearning\DataPreprocessing.fs" />
    <Compile Include="MachineLearning\ModelSerialization.fs" />
    <Compile Include="MachineLearning\QuantumRegressionHHL.fs" />
    <Compile Include="MachineLearning\HHLModelSerialization.fs" />

    <!-- Business-First ML Builders -->
    <Compile Include="Business\BinaryClassificationBuilder.fs" />
    <Compile Include="Business\AnomalyDetectionBuilder.fs" />
    <Compile Include="Business\SimilaritySearchBuilder.fs" />
    <Compile Include="Business\PredictiveModelBuilder.fs" />
    <Compile Include="Business\AutoMLBuilder.fs" />
    <Compile Include="Business\QuantumRiskEngine.fs" />
    <Compile Include="Business\QuantumDrugDiscovery.fs" />

    <!-- Business-First Quantum Solvers -->
    <Compile Include="Business\SocialNetworkAnalyzer.fs" />
    <Compile Include="Business\ConstraintScheduler.fs" />
    <Compile Include="Business\CoverageOptimizer.fs" />
    <Compile Include="Business\ResourcePairing.fs" />
    <Compile Include="Business\PackingOptimizer.fs" />

    <!-- C# Interop Extensions -->
    <Compile Include="Builders\BuildersCSharpExtensions.fs" />
  </ItemGroup>

  <ItemGroup>
    <!-- Compiled C# Business Builder Wrappers DLL -->
    <None Include="Business\CSharp\bin\$(Configuration)\net10.0\FSharp.Azure.Quantum.Business.CSharp.dll" Pack="true" PackagePath="lib\net10.0\" />
    <None Include="Business\CSharp\bin\$(Configuration)\net10.0\FSharp.Azure.Quantum.Business.CSharp.xml" Pack="true" PackagePath="lib\net10.0\" Condition="Exists('Business\CSharp\bin\$(Configuration)\net10.0\FSharp.Azure.Quantum.Business.CSharp.xml')" />

    <!-- C# Global Usings (auto-enables extensions for .NET 10 C# projects) -->
    <None Include="GlobalUsings.cs" Pack="true" PackagePath="contentFiles\cs\net10.0\GlobalUsings.cs" />

  </ItemGroup>


  <ItemGroup>
    <!-- NOTE: FSharp.Core is implicitly included by the F# SDK and automatically
         flows to NuGet package consumers as a transitive dependency.
         Current version: 10.0.100 (from .NET 10 SDK)
         C# projects consuming this library will automatically get FSharp.Core. -->

    <PackageReference Include="Azure.Identity" Version="1.21.0" />
    <PackageReference Include="MathNet.Numerics" Version="5.0.0" />
    <PackageReference Include="MathNet.Numerics.FSharp" Version="5.0.0" />
    <PackageReference Include="Microsoft.Azure.Quantum.Client" Version="0.28.302812" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.10" />
    <PackageReference Include="Serilog" Version="4.4.0" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.1.1" />
    <PackageReference Include="Serilog.Sinks.File" Version="7.0.0" />
    <PackageReference Include="OpenTelemetry" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Exporter.Console" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Http" Version="1.17.0" />
  </ItemGroup>


  <ItemGroup>
    <PackageReference Update="FSharp.Core" Version="10.1.302" />
    <PackageReference Update="Microsoft.CodeAnalysis.NetAnalyzers" Version="10.0.302" />
    <PackageReference Update="SonarAnalyzer.CSharp" Version="10.31.0.145097" />
  </ItemGroup>

</Project>
